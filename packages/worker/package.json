{
  "name": "@hookpipe/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "db:migrate:local": "wrangler d1 migrations apply hookpipe-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply hookpipe-db --remote"
  },
  "dependencies": {
    "@hookpipe/providers": "workspace:*",
    "@hookpipe/shared": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.7.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.21",
    "@cloudflare/workers-types": "^4.20250313.0",
    "@vitest/coverage-istanbul": "^3.2.4",
    "drizzle-kit": "^0.31.9",
    "vitest": "^3.2.4",
    "wrangler": "^4.0.0"
  }
}
